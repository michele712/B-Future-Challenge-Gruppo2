{
  "name": "my-fastapi-app",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "axios": "^0.24.0",
    "chart.js": "^3.9.0",
    "react": "^18.3.1",
    "react-chartjs-2": "^3.0.4",
    "react-dom": "^18.3.1",
    "react-scripts": "5.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "start-backend": "uvicorn app:app --host 0.0.0.0 --port $PORT"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
